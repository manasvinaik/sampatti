import 'package:flutter/material.dart';
class PropertyInsurancePage extends StatelessWidget {
  const PropertyInsurancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Property Insurance')),
      body: const Center(child: Text('Property Insurance Page')),
    );
  }
}
